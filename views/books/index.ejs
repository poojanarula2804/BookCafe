<% layout('layouts/boilerplate') %> 

<header>
<text style="font-size: 2em;font-weight: bold; margin-left: 400px"> Explore the collection of books </text> 
    <!-- <div  style="float:right; "><a href="/books/new" class="btn btn-secondary btn-md active" role="button" aria-pressed="true">Add Book</a> -->
 <!-- <button class="btn btn-secondary"> <a href="/books/new">Add Campground</a> </button> -->
</div></header>
<!-- <p>
    <form action="/books" method="GET" class="form-inline" id="campground-search">
      <div class="form-group">
        <input type="text" name="search" placeholder="Search Books..." class="form-control">
      </div>
    </form>
   </p> -->
<div class="row mt-1 justify-content-center text-center">
<%for (let campground of campgrounds){%>
    <div class="card col-md-4 mt-2 mb-2 ml-3">
        <!-- <div class="row"> -->
            <div class="card-body d-flex flex-column"> 
                    <img class="img-fluid" src="<%= campground.images[0].thumbnail %>">
                    <h5 class="card-title pt-4">
                        <%= campground.title %> 
                    </h5>
                    <p class="card-text pt-1">
                        <%= campground.description %> 
                    </p>
                    <p class="card-text">
                        <small class="text-muted">Genre: <%= campground.genre %> </small> 
                    </p>
                    <a class="btn btn-primary mt-auto" href="/books/<%= campground._id %> "> View <%= campground.title %> </a>
            </div>
        <!--</div> -->
    </div>  
<%}%>
</div>
